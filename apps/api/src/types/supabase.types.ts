/**
 * Re-exported from @inventory-mgmt/database, the single source of truth for
 * the generated/hand-written Supabase schema types (kept in sync with
 * packages/database/supabase/migrations).
 */
export type { Database, Json } from '@inventory-mgmt/database';
